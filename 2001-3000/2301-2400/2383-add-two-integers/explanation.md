# Explanation

## Idea

Return `num1 + num2` directly.

## Why It Works

The requested result is exactly the arithmetic sum of the two inputs, and the constraints fit easily in `int`.

## Edge Cases

- Negative values are handled by normal integer addition.
- Adding zero returns the other operand.

## Complexity

Time: `O(1)`.

Space: `O(1)`.
